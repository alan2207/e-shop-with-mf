{
  "name": "e-shop-showcase-mf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run --parallel dev",
    "start": "lerna run --parallel start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Alickovic",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
